name: Formatting check
on:
  - pull_request

jobs:
  black:
    runs-on: ubuntu-latest

    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.4.1
        with:
          access_token: ${{ github.token }}

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Checkout
        uses: actions/checkout@v2

      - name: Set env
        run: |
          echo "CHANGED_SRC_FILES=$(git diff --name-only --diff-filter=ACMRTUXB origin/master | grep -E 'pennylane/')" >> $GITHUB_ENV
          echo "CHANGED_TEST_FILES=$(git diff --name-only --diff-filter=ACMRTUXB origin/master | grep -E 'pennylane/')" >> $GITHUB_ENV

      - name: Install dependencies
        run: pip install black

      - name: Run Black
        run: |
          echo "------------------------------------"
          echo "Running black on files: $CHANGED_SRC_FILES"
          if [[ $CHANGED_SRC_FILES != "" ]]
          then black -l 100 $CHANGED_SRC_FILES --check
          fi
          echo "------------------------------------"
          echo "Running black on files: $CHANGED_TEST_FILES"
          if [[ $CHANGED_TEST_FILES != "" ]];
          then black -l 100 $CHANGED_TEST_FILES --check
          fi
