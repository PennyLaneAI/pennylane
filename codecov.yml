ignore:
  - "pennylane/devices/default_mixed.py"
  - "pennylane/devices/tests/*"
  - "pennylane/data/base/_lazy_modules.py"
  - "pennylane/logging/*"

codecov:
  notify:
    after_n_builds: 1

comment:
  after_n_builds: 1
coverage:

  status:
    project:
      default:
        threshold: 0.02%
