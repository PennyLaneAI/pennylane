[pytest]
markers =
    skip_unsupported: skip a test if it uses an operation unsupported on a device
    benchmarks: marks tests that include benchmarks (select with '-m "benchmarks"')
addopts = --benchmark-disable